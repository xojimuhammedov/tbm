import type { FieldValues } from 'react-hook-form';
import { type FormItemProps, type InputProps } from 'dgz-ui/form';
import { type CalendarProps } from 'dgz-ui/calendar';
export type MyDatePickerProps<TFieldValues extends FieldValues> = FormItemProps<TFieldValues> & Omit<InputProps, 'onSelect'> & Omit<CalendarProps, 'mode'> & {
    format?: string;
};
/**
 * MyDatePicker shows a calendar popover to pick a single date, integrated with react-hook-form.
 * Can also be used standalone when no control/name are provided.
 *
 * @template TFieldValues - Form values type used by react-hook-form.
 * @param props - Calendar, button and form item props.
 */
export declare const MyDatePicker: <TFieldValues extends FieldValues>({ control, name, label, helperText, required, rules, format, placeholder, floatingError, register, ...props }: MyDatePickerProps<TFieldValues>) => import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=MyDatePicker.d.ts.map