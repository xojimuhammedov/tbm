// interface HasAccessProps {
//     roles: string[];
//     children?: React.ReactNode;
// }

const HasAccess = () => {
  /* // const { me } = useUserStore();
     //
     // return <>{includes(roles, get(me, 'role')) ? children : <Navigate to={'/error/forbidden'} />}</>;*/
};

export default HasAccess;
